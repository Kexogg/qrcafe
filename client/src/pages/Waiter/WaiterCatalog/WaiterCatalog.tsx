import { Catalog } from '../../../components/UI/Catalog/Catalog.tsx'

export const WaiterCatalog = () => {
    return (
        <section>
            <Catalog title={'Меню'} />
        </section>
    )
}
